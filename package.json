{
  "scripts": {
    "build": "elm make ./src/Main.elm --output ./dist/app.js",
    "prettify": "elm-format src/Main.elm",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-format": "^0.6.1-alpha",
    "gh-pages": "^1.0.0"
  }
}
